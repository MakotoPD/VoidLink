## Changes (v0.3.14)

### 🐛 Bug Fixes
- **CPU Usage Display**: Normalized CPU usage to 0-100% range by dividing by the number of CPU cores. Previously showed values like 500%+ on multi-core systems.

## Changes (v0.3.13)

### 🐛 Bug Fixes
- **Linux CPU/RAM Monitoring**: Fixed CPU usage always showing 0% on Linux. The sysinfo library requires two measurement samples with a time delay to calculate CPU usage correctly.

## Changes (v0.3.12)

### 🐛 Bug Fixes
- **Auto-Update**: Final fix for artifact signing configuration.

## Changes (v0.3.11)

### 🐛 Bug Fixes
- **Auto-Update**: Release pipeline configuration changes to resolve artifacts signing issues.

## Changes (v0.3.10)

### 🐛 Bug Fixes
- **Auto-Update**: Fixed signing configuration to correctly handle empty key passwords in the build pipeline.

## Changes (v0.3.9)

### 🐛 Bug Fixes
- **Update Signing**: Regenerated signing keys and fixed GitHub Actions workflow to ensure auto-update artifacts (`.json` and `.sig`) are correctly generated and published.

## Changes (v0.3.8)

## Changes (v0.3.7)

### 🐛 Bug Fixes
- **Linux Java Download**: Fixed an issue where downloading Java on Linux failed with "unsupported archive format: gz". Added support for `.tar.gz` extraction.
- **Dark Mode**: Fixed inconsistent dark theme application for Modals and Toasts.

## Changes (v0.3.6)

### ✨ Features & Improvements
- **Enhanced Java Detection**: The `Auto-Detect` feature now intelligently scans the application's local data directory. Java versions downloaded via VoidLink are now automatically discovered and configured without manual path selection.
- **Better Installation Feedback**: Added clear visual feedback during Java installation, including exact download paths and success/failure system notifications (Toasts).

### 🐛 Bug Fixes
- **Linux/Tray Logic**: Fixed a race condition that prevented the application from closing correctly on Linux (and other platforms) when "Minimize to Tray" was disabled.

### 📦 Maintenance
- **Internal**: Updated Rust-side detection logic to support app-local Java paths.
